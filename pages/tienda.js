import Layout from "../components/Layout";

const tienda = () => {
  return (
    <div>
      <Layout pagina={"Tienda"}>
        <div className="heading">Desde tienda</div>
      </Layout>
    </div>
  );
};

export default tienda;
